# Render Blueprint: https://render.com/docs/blueprint-spec
# Deploy: New → Blueprint → connect this repo → apply (set EMAIL_* in dashboard).
version: "1"
services:
  - type: web
    name: govsecure
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: LOG_CONSOLE
        value: "true"
      - key: DATABASE_PATH
        value: /usr/src/app/data/database.sqlite
      - key: 6077908635
        generateValue: true
      - key: kshitijrana1917@gmail.com
        sync: false
      - key: gecugbcfxfxzcluo
        sync: false
